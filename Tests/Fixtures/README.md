# Test Fixtures

Test fixtures for the seam-carving package are generated deterministically at
runtime; no copyrighted images are committed to the repository.

See `manifest.json` for the set of fixtures exercised by the golden-corpus
workflow.
